import React from 'react'
import './project-item.css'

function ProjectItem({ project }) {
    // const { id, name, image, state } = project;

    return (
        <div className='project-item'>

        </div>
    )
}

export default ProjectItem; 